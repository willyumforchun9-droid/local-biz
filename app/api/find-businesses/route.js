export async function POST(request) { const { city, type } = await request.json(); const searchResponse = await fetch(`https://maps.googleapis.com/maps/api/place/textsearch/json?query=${type}+in+${city}&key=${process.env.GOOGLE_PLACES_API_KEY}`); const searchData = await searchResponse.json(); const businesses = []; for (const place of searchData.results.slice(0, 10)) { const detailsResponse = await fetch(`https://maps.googleapis.com/maps/api/place/details/json?place_id=${place.place_id}&fields=name,formatted_address,formatted_phone_number,website,rating&key=${process.env.GOOGLE_PLACES_API_KEY}`); const detailsData = await detailsResponse.json(); const details = detailsData.result; businesses.push({ name: details.name, address: details.formatted_address, phone: details.formatted_phone_number || "N/A", website: details.website || null, rating: details.rating || "N/A", hasWebsite: !!details.website }); } return Response.json({ businesses, noWebsite: businesses.filter(b => !b.hasWebsite) }); }
